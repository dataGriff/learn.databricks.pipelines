#!/bin/bash
# brew install azure-cli
# brew tap databricks/tap
# brew install databricks
# brew install java
# brew install apache-spark

# ##pip install pyspark
# pip install databricks-connect
# pip install jupyter

TODO: how much of these tools do I now have installed and which ones do I need that are missing?